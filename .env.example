# PostgreSQL (Docker)
DATABASE_URL=postgresql://menro:menro_password@localhost:5433/bantay_kalikasan

# API Server
PORT=3001
JWT_SECRET=bantay-kalikasan-menro-rizal-2026

# Web App (Vite dev proxy targets API at localhost:3001)
VITE_API_URL=/api

# Intranet example (other devices on LAN — use your server PC IP)
# VITE_API_URL=http://192.168.1.10:3001/api

# Desktop App (Electron — use the PC that runs PostgreSQL/API, not localhost on each device)
# API_URL=http://192.168.1.10:3001/api
